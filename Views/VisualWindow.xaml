<Window x:Class="AudioVisualizer.Views.VisualWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AudioVisualizer.Views"
        xmlns:controls="clr-namespace:AudioVisualizer.Controls"
        xmlns:vm="clr-namespace:AudioVisualizer.ViewModels"
        mc:Ignorable="d"
        Title="Visualizer" Height="450" Width="800"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        SourceInitialized="Window_SourceInitialized" Closing="Window_Closing"
        Topmost="{Binding AlwaysOnTop}">
    
    <Grid>
        <Border Background="#FF101010" CornerRadius="5" BorderBrush="#FF444444" BorderThickness="1">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Custom Title Bar -->
                <Border Grid.Row="0" Background="#252525" CornerRadius="5,5,0,0" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown" Height="30">
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button Content="_" Width="30" Click="Minimize_Click" Style="{x:Null}"/>
                        <Button Content="[ ]" Width="30" Click="Maximize_Click" Style="{x:Null}"/>
                        <Button Content="X" Width="30" Click="Close_Click" Style="{x:Null}"/>
                    </StackPanel>
                </Border>

                <!-- Visual Content -->
                <Grid Grid.Row="1">
                    <TextBlock Text="No preset selected or preset not found." Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Visibility" Value="Visible"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding SelectedPreset}" Value="Particles">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding SelectedPreset}" Value="Flow Field">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding SelectedPreset}" Value="Audio Grid">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding SelectedPreset}" Value="Wave Pattern">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding SelectedPreset}" Value="OpenGL Visual">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding SelectedPreset}" Value="Veldrid Visual">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                    <controls:ParticlesVisual>
                        <controls:ParticlesVisual.Style>
                            <Style TargetType="controls:ParticlesVisual">
                                <Setter Property="Visibility" Value="Collapsed"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding SelectedPreset}" Value="Particles">
                                        <Setter Property="Visibility" Value="Visible"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </controls:ParticlesVisual.Style>
                    </controls:ParticlesVisual>
                    <controls:FlowFieldVisual>
                        <controls:FlowFieldVisual.Style>
                            <Style TargetType="controls:FlowFieldVisual">
                                <Setter Property="Visibility" Value="Collapsed"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding SelectedPreset}" Value="Flow Field">
                                        <Setter Property="Visibility" Value="Visible"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </controls:FlowFieldVisual.Style>
                    </controls:FlowFieldVisual>
                    <controls:AudioGridVisual>
                        <controls:AudioGridVisual.Style>
                            <Style TargetType="controls:AudioGridVisual">
                                <Setter Property="Visibility" Value="Collapsed"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding SelectedPreset}" Value="Audio Grid">
                                        <Setter Property="Visibility" Value="Visible"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </controls:AudioGridVisual.Style>
                    </controls:AudioGridVisual>
                    <controls:WavePatternVisual>
                        <controls:WavePatternVisual.Style>
                            <Style TargetType="controls:WavePatternVisual">
                                <Setter Property="Visibility" Value="Collapsed"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding SelectedPreset}" Value="Wave Pattern">
                                        <Setter Property="Visibility" Value="Visible"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </controls:WavePatternVisual.Style>
                    </controls:WavePatternVisual>
                    <controls:OpenGLVisual>
                        <controls:OpenGLVisual.Style>
                            <Style TargetType="controls:OpenGLVisual">
                                <Setter Property="Visibility" Value="Collapsed"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding SelectedPreset}" Value="OpenGL Visual">
                                        <Setter Property="Visibility" Value="Visible"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </controls:OpenGLVisual.Style>
                    </controls:OpenGLVisual>
                    <controls:VeldridVisual>
                        <controls:VeldridVisual.Style>
                            <Style TargetType="controls:VeldridVisual">
                                <Setter Property="Visibility" Value="Collapsed"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding SelectedPreset}" Value="Veldrid Visual">
                                        <Setter Property="Visibility" Value="Visible"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </controls:VeldridVisual.Style>
                    </controls:VeldridVisual>
                </Grid>
            </Grid>
        </Border>
    </Grid>
</Window>
